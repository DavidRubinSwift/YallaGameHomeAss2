


/*using UnityEngine;

public class GameDataTest : MonoBehaviour
{
    public SaveManager saveManager;

    void Start()
    {
        // Пример: создаём объект с данными и сохраняем его
        SaveData data = new SaveData();
        data.playerName = "David";
        data.score = 123;

        saveManager.SaveGame(data);

        // Пример: загружаем данные и выводим в консоль
        SaveData loaded = saveManager.LoadGame();
        if (loaded != null)
        {
            Debug.Log("Loaded Player: " + loaded.playerName + ", Score: " + loaded.score);
        }
    }
}*/
